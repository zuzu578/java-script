//재귀함수의 사용 예시//
//재귀함수란 함수 정의문 내에서 작성한코드로 함수를 다시호출할때 사용한다.//
//즉 선언한 함수 자기자신을 호출하는 방법으로//
//함수를 반복문처럼 사용할때 쓴다//


/*function testFnc(){
  for(var i = 0 ; i<=10 ; i++){
    if(i==10) {
      return;
    }
    document.write(i);
  }
  testFnc();
}
testFnc();
*/